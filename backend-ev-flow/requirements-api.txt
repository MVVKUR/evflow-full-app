# Slim RUNTIME dependencies for the FastAPI API container (no analysis/geo stack).
# The notebook's heavy deps (geopandas, osmnx, matplotlib, jupyter, kaggle, …) live in
# requirements.txt and are NOT needed to serve the API. osmnx is only needed offline to
# *build* the road graph (scripts/build_road_graph.py); at runtime networkx reads the
# cached GraphML.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
numpy>=1.26
pyyaml>=6.0
networkx>=3.0
sqlalchemy>=2.0
psycopg[binary]>=3.1
alembic>=1.13
httpx>=0.27
bcrypt>=4.1
PyJWT>=2.8
